name: Deploy Ezequiel Portfolio

on:
  push:
    branches: [main]
    paths:
      - "ezequiel-vecchio/**"
      - ".github/workflows/deploy-ezequiel-portfolio.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload portfolio
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./ezequiel-vecchio

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
